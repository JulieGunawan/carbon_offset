type Invoice {
  orderId: Int!
  amount: Int!
  treePurchased: Int!
  fkVehicleId: Int!
  fkUserId: Int!
  createdAt:Date!
  updatedAt:Date!
  deletedAt:Date
}

type Query {
  getAllInvoices: [Invoice]
  getOneInvoiceById(orderId: Int!): Invoice
}