type Invoice {

  orderId: Int!
  amount: Int!
  treePurchased: Int!
  fkUserId: Int!
  fkVehicleId: Int!
  createdAt:Date!
  updatedAt:Date!
  deletedAt:Date
}

type Query {
  getAllInvoices: [Invoice]
  getOneInvoice(orderId: Int!): Invoice
}